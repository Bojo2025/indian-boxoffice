# Copy this file to ".env" in the project root. Do not commit .env.

# Desk analyst + AI daily report (https://console.x.ai)
XAI_API_KEY=

# Optional. Unset = embedded PGLite (fine for local).
# Production: a Neon / Postgres connection string.
# DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# Optional. Shown on /download as the GitHub button.
# VITE_GITHUB_REPO=https://github.com/<you>/indian-box-office
